export interface IWatcher {
    trigger: (props?: any) => {};
    id: number;
}
export declare class WatchableStore<T> {
    private _watchers;
    private _data;
    private _nextHandlerId;
    constructor(initialData: T);
    data: T;
    watch(cb: (data: T) => any): number;
    unwatch(id: number): void;
}
